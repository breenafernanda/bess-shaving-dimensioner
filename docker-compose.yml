version: '3.8'
services:
  bess:
    container_name: BESS
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: sh -c "pnpm install && pnpm dev"
